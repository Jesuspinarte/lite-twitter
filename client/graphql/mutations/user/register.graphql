mutation Register($user: UserInput!) {
  register(user: $user) {
    errors {
      field
      message
    }
    user {
      id
      name
      username
      email
      createdAt
    }
  }
}
